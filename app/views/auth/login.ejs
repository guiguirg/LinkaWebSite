<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <meta name="description" content="<%= description %>">
    <meta name="keywords" content="<%= keywords %>">
    
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/public/images/logo.png">
    
    <!-- CSS -->
    <link rel="stylesheet" href="/public/css/main.css">
    <link rel="stylesheet" href="/public/css/components.css">
    <link rel="stylesheet" href="/public/css/auth.css">
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body class="<%= page %>-page">
    <%- include('../partials/header') %>
    
    <main>
        <!-- Login Section -->
        <section class="auth-section">
            <div class="container">
                <div class="auth-container">
                    <div class="auth-content">
                        <div class="auth-header">
                            <h1 class="auth-title">Bem-vindo de volta!</h1>
                            <p class="auth-description">
                                Entre na sua conta e continue recebendo pedidos
                            </p>
                        </div>
                        
                        <form class="auth-form" id="loginForm">
                            <div class="form-group">
                                <label class="form-label">Entrar como:</label>
                                <div class="user-type-toggle">
                                    <button type="button" class="toggle-btn active" data-type="prestador">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                            <path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
                                        </svg>
                                        Prestador
                                    </button>
                                    <button type="button" class="toggle-btn" data-type="cliente">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                            <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/>
                                            <circle cx="9" cy="7" r="4"/>
                                            <path d="M22 21v-2a4 4 0 0 0-3-3.87"/>
                                            <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
                                        </svg>
                                        Cliente
                                    </button>
                                </div>
                                <input type="hidden" id="userType" name="userType" value="prestador">
                            </div>

                            <div class="form-group">
                                <label for="email" class="form-label">E-mail</label>
                                <input 
                                    type="email" 
                                    id="email" 
                                    name="email" 
                                    class="form-input" 
                                    placeholder="seu@email.com"
                                    required
                                >
                            </div>
                            
                            <div class="form-group">
                                <label for="password" class="form-label">Senha</label>
                                <input 
                                    type="password" 
                                    id="password" 
                                    name="senha" 
                                    class="form-input" 
                                    placeholder="••••••••"
                                    required
                                >
                            </div>
                            
                            <div class="form-row form-options">
                                <div class="form-check">
                                    <input type="checkbox" id="remember" name="remember" class="form-checkbox">
                                    <label for="remember" class="form-check-label">Lembrar de mim</label>
                                </div>
                                <a href="#" class="form-link">Esqueceu a senha?</a>
                            </div>
                            
                            <button type="submit" class="btn btn-primary btn-lg btn-full" id="loginBtn">
                                Entrar
                            </button>
                            
                            <div class="form-footer">
                                <p>Ainda não tem conta? <a href="/cadastrar" class="form-link-strong">Cadastre-se grátis</a></p>
                            </div>
                        </form>
                        
                        <div class="auth-benefits">
                            <div class="benefit-item">
                                <i data-lucide="check-circle"></i>
                                <span>Receba pedidos na sua região</span>
                            </div>
                            <div class="benefit-item">
                                <i data-lucide="check-circle"></i>
                                <span>Você define seus preços</span>
                            </div>
                            <div class="benefit-item">
                                <i data-lucide="check-circle"></i>
                                <span>Construa sua reputação</span>
                            </div>
                        </div>
                    </div>
                    
                    <div class="auth-image">
                        <img src="/public/images/servicos.png" alt="Profissionais Linka" class="auth-img">
                    </div>
                </div>
            </div>
        </section>
    </main>
    
    <%- include('../partials/footer') %>
    
    <style>
        .user-type-toggle {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.5rem;
            margin-top: 0.5rem;
        }
        
        .toggle-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            padding: 0.875rem 1rem;
            border: 2px solid #e2e8f0;
            background: white;
            border-radius: 0.75rem;
            font-size: 0.9375rem;
            font-weight: 600;
            color: #64748b;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .toggle-btn svg {
            transition: all 0.3s ease;
        }
        
        .toggle-btn:hover {
            border-color: #ff6b35;
            color: #ff6b35;
            transform: translateY(-2px);
        }
        
        .toggle-btn.active {
            background: linear-gradient(135deg, #ff6b35 0%, #ff8c61 100%);
            border-color: #ff6b35;
            color: white;
        }
    </style>

    <!-- Lucide Icons Script -->
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
    
    <script>
        // Inicializa Lucide Icons
        if (typeof lucide !== 'undefined') {
            lucide.createIcons();
        }
        
        // Aguarda DOM estar pronto
        document.addEventListener('DOMContentLoaded', function() {
            console.log('Login page loaded');
            
            // Toggle user type
            const toggleBtns = document.querySelectorAll('.toggle-btn');
            const userTypeInput = document.getElementById('userType');
            
            console.log('Toggle buttons found:', toggleBtns.length);
            
            // Salvar o tipo selecionado no localStorage
            const savedUserType = localStorage.getItem('loginUserType');
            if (savedUserType) {
                userTypeInput.value = savedUserType;
                toggleBtns.forEach(btn => {
                    if (btn.dataset.type === savedUserType) {
                        btn.classList.add('active');
                    } else {
                        btn.classList.remove('active');
                    }
                });
            }
            
            toggleBtns.forEach(btn => {
                btn.addEventListener('click', (e) => {
                    e.preventDefault();
                    console.log('Toggle clicked:', btn.dataset.type);
                    toggleBtns.forEach(b => b.classList.remove('active'));
                    btn.classList.add('active');
                    userTypeInput.value = btn.dataset.type;
                    localStorage.setItem('loginUserType', btn.dataset.type);
                });
            });
            
            // Handle login form
            const loginForm = document.getElementById('loginForm');
            const loginBtn = document.getElementById('loginBtn');
            
            console.log('Form found:', loginForm ? 'yes' : 'no');
            console.log('Button found:', loginBtn ? 'yes' : 'no');
            
            if (loginForm) {
                loginForm.addEventListener('submit', async (e) => {
                    e.preventDefault();
                    console.log('Form submitted');
                    
                    // Desabilitar botão mas não alterar texto ainda
                    loginBtn.disabled = true;
                    const originalText = loginBtn.textContent;
                    loginBtn.innerHTML = '<span style="display: inline-flex; align-items: center; gap: 8px;"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="animation: spin 1s linear infinite;"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg> Entrando...</span>';
                    
                    const formData = {
                        email: document.getElementById('email').value.trim(),
                        senha: document.getElementById('password').value,
                        userType: document.getElementById('userType').value
                    };
                    
                    console.log('Sending login request:', { email: formData.email, userType: formData.userType });
                    
                    try {
                        const response = await fetch('/entrar', {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify(formData)
                        });
                        
                        console.log('Response status:', response.status);
                        const data = await response.json();
                        console.log('Response data:', data);
                        
                        if (data.success) {
                            console.log('Login successful, redirecting to:', data.redirect);
                            loginBtn.innerHTML = '<span style="display: inline-flex; align-items: center; gap: 8px;">✓ Sucesso! Redirecionando...</span>';
                            loginBtn.style.background = 'linear-gradient(135deg, #10b981 0%, #059669 100%)';
                            
                            // Aguardar um momento antes de redirecionar para o usuário ver o feedback
                            setTimeout(() => {
                                window.location.href = data.redirect;
                            }, 500);
                        } else {
                            // Mostrar erro específico
                            const errorMsg = data.mensagem || 'Erro ao fazer login';
                            
                            // Criar elemento de erro se não existir
                            let errorDiv = document.querySelector('.login-error');
                            if (!errorDiv) {
                                errorDiv = document.createElement('div');
                                errorDiv.className = 'login-error';
                                errorDiv.style.cssText = 'background: linear-gradient(135deg, #fee2e2 0%, #fecaca 100%); border: 1px solid #f87171; color: #991b1b; padding: 1rem; border-radius: 12px; margin-bottom: 1rem; font-size: 0.95rem; display: flex; align-items: center; gap: 0.75rem;';
                                loginForm.insertBefore(errorDiv, loginForm.firstChild);
                            }
                            
                            errorDiv.innerHTML = '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="15" y1="9" x2="9" y2="15"></line><line x1="9" y1="9" x2="15" y2="15"></line></svg><div><strong>Erro no login:</strong><br>' + errorMsg + '</div>';
                            
                            // Destacar o botão do tipo correto
                            loginBtn.style.background = '';
                            const userTypeName = formData.userType === 'prestador' ? 'Prestador' : 'Cliente';
                            console.error('Login failed for:', userTypeName);
                            
                            loginBtn.disabled = false;
                            loginBtn.textContent = originalText;
                        }
                    } catch (error) {
                        console.error('Login error:', error);
                        
                        // Criar elemento de erro se não existir
                        let errorDiv = document.querySelector('.login-error');
                        if (!errorDiv) {
                            errorDiv = document.createElement('div');
                            errorDiv.className = 'login-error';
                            errorDiv.style.cssText = 'background: linear-gradient(135deg, #fee2e2 0%, #fecaca 100%); border: 1px solid #f87171; color: #991b1b; padding: 1rem; border-radius: 12px; margin-bottom: 1rem; font-size: 0.95rem; display: flex; align-items: center; gap: 0.75rem;';
                            loginForm.insertBefore(errorDiv, loginForm.firstChild);
                        }
                        
                        errorDiv.innerHTML = '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="15" y1="9" x2="9" y2="15"></line><line x1="9" y1="9" x2="15" y2="15"></line></svg><div><strong>Erro de conexão:</strong><br>Não foi possível conectar ao servidor. Tente novamente.</div>';
                        
                        loginBtn.disabled = false;
                        loginBtn.textContent = originalText;
                    }
                });
            }
        });
    </script>
    
    <style>
        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
    </style>
</body>
</html>
